/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['silaaimachines.com', 'localhost', '192.168.1.10']
    }
};

export default nextConfig;
